# Application environment example
# Copy to .env and adjust values as needed.

# Prisma/PostgreSQL connection URL (entrypoint will auto-generate if unset)
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# DATABASE_URL=

# Toggle PostGIS extension creation (handled at container start)
# POSTGIS_ENABLE=1

# Caddy production domain (omit in local dev). If set, Caddy will request certs.
# DOMAIN=example.com

# Node environment
NODE_ENV=development
