# 開発用環境変数サンプル (必要に応じて複製して .env を作成)
# 秘匿情報は本番では別管理 (Secrets Manager / Vault 等)

# Prisma / アプリ DB 接続文字列
DATABASE_URL=postgresql://appuser:appsecret@localhost:5432/appdb?schema=public

# PostGIS を無効化したい場合 docker-compose.yml で POSTGIS_ENABLE=0 に設定
# POSTGIS_ENABLE=1

# Next.js 追加環境変数例 (未使用)
# NEXT_PUBLIC_API_BASE=https://localhost/api
